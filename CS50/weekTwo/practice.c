#include <stdio.h>

int main() {
    const int N = 3;
    //prototype
    float average(int length, int array[]);
    int main(){
        int socre[N];
        for (int i =0; i<N; i++){
            scores[i] = get
    
    float average(int length, int array[])
    {
        // calculate average
        int sum = 0;
        for(int i =0; i<length; i++){
            {
                sum+=array[i];
            }
            return sum / (float) length;

    return 0;
}
